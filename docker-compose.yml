services:
  backend:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - CLIENT_URL=http://localhost:80

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend